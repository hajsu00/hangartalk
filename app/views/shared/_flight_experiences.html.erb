<% @user ||= current_user %>
<div class="flight_experiences">
  <h3>飛行経歴</h3>
  <% if @user.aeroplane_flights.any? %>
    
    <h3><a href="#">飛行機</a></h3>
    <ul>
      <li>総飛行時間<%= @user.aeroplane_flights.find_by(id: 1) %></li>
      <li>総離着陸回数</li>
      <li>機長時間</li>
    </ul>
  <% end %>
</div>