<%= form_with(model: current_user.active_relationships.build, local: false) do |f| %>
  <%= hidden_field_tag :followed_id, @user.id %>
  <%= f.submit "フォローする", class: 'cursor-pointer bg-indigo-600 text-white active:bg-white active:text-indigo-600 font-bold hover:shadow-md shadow text-xs px-4 py-2 rounded border-solid border-2 border-indigo-600 sm:mr-2 mb-1 ease-linear transition-all duration-150' %>
<% end %>
