<%= form_with(model: current_user.group_users.build, local: false) do |f| %>
  <%= hidden_field_tag :group_id, params[:id] %>
  <%= f.submit "グループに参加", class: "cursor-pointer bg-indigo-700 hover:bg-indigo-600 active:bg-indigo-500 text-white font-bold hover:shadow-md shadow text-xs px-4 py-2 rounded outline-none focus:outline-none sm:mr-2 mb-1 ease-linear transition-all duration-150" %>
<% end %>
