<%= form_with(model: current_user.active_relationships.find_by(followed_id: @user.id), html: { method: :delete }, local: false) do |f| %>
  <%= hidden_field_tag :followed_id, @user.id %>
  <%= f.submit "フォロー中", class: 'cursor-pointer bg-white active:bg-indigo-600 active:text-white text-indigo-600 font-bold hover:shadow-md shadow text-xs px-4 py-2 rounded border-solid border-2 border-indigo-600 sm:mr-2 mb-1 ease-linear transition-all duration-150' %>
<% end %>